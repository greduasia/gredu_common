name: gredu_common
description: GreduUI is private packages for Gredu company, not commersial purpose.

version: 0.1.6
homepage: https://github.com/greduasia/gredu_common

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  clock: ^1.1.0
  device_info_plus: ^8.0.0
  flutter_svg: ^1.0.3
  get: ^4.6.1
  intl: ^0.17.0
  meta: ^1.7.0
  velocity_x: ^3.6.0

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/